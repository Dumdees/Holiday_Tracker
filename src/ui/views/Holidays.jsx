// TODO: implemented by a later stage. Keep the export name.
export function Holidays({ params }) {
  return (
    <div class="page">
      <div class="page-head"><div><h1>Holidays</h1><p class="lede">Add, change or remove holidays</p></div></div>
      <p class="muted">This section is coming next.</p>
    </div>
  );
}
