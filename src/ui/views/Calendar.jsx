// TODO: implemented by a later stage. Keep the export name.
export function Calendar({ params }) {
  return (
    <div class="page">
      <div class="page-head"><div><h1>Calendar</h1><p class="lede">See who is off and when</p></div></div>
      <p class="muted">This section is coming next.</p>
    </div>
  );
}
