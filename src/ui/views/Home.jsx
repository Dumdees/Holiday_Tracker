// TODO: implemented by a later stage. Keep the export name.
export function Home({ params }) {
  return (
    <div class="page">
      <div class="page-head"><div><h1>Home</h1><p class="lede">Today at a glance</p></div></div>
      <p class="muted">This section is coming next.</p>
    </div>
  );
}
