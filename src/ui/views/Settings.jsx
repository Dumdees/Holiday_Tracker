// TODO: implemented by a later stage. Keep the export name.
export function Settings({ params }) {
  return (
    <div class="page">
      <div class="page-head"><div><h1>Settings</h1><p class="lede">Holiday year, teams, backups</p></div></div>
      <p class="muted">This section is coming next.</p>
    </div>
  );
}
